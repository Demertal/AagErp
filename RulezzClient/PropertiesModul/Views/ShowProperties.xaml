<UserControl x:Class="PropertiesModul.Views.ShowProperties"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:interactivity="http://prismlibrary.com/"
             interactivity:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             VerticalAlignment="Top" VerticalContentAlignment="Top" HorizontalContentAlignment="Left" HorizontalAlignment="Left">
    <StackPanel>
        <TextBlock Text="{Binding StringFormat=Группа: {0}, Path=Group.Title}"/>
        <StackPanel Orientation="Horizontal">
            <ListView MinWidth="100" MinHeight="100" Margin="5" ItemsSource="{Binding PropertyNames}" SelectedItem="{Binding Path=PropertyName}">
                <ListView.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Добавить"></MenuItem>
                        <MenuItem Header="Изменить"></MenuItem>
                        <MenuItem Header="Удалить"></MenuItem>
                    </ContextMenu>
                </ListView.ContextMenu>
            </ListView>
            <ListView MinWidth="100" MinHeight="100" Margin="5"/>
        </StackPanel>
    </StackPanel>
</UserControl>
